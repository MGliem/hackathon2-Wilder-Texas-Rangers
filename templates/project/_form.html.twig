
{{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="w-100 d-flex justify-content-between">
     <a class="btn btn-lg mt-1 btn-outline-secondary" href="{{ path('project_index') }}">back to list</a>
     <button class="btn btn-lg mt-1 btn-secondary">{{ button_label|default('Save') }}</button>
     </div>
{{ form_end(form) }}

